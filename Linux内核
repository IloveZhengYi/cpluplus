[内存管理]

❓ MMAP是什么? 原理? 为什么MMAP比read/write快?
- 是一种内存映射文件的机制, 将文件映射到虚拟地址空间, 使进程可以像内存一般读写文件, 不用频繁read/write系统调用 .
❓ 传统read/write的缺点:
- 涉及2次内存拷贝: 磁盘 -> 内核态(Page Cache) -> 用户态 (反之亦然), 涉及到多次用户态和内核态切换, 效率低.
- MMAP的优点: mmap消除了page cache到用户空间的额外拷贝, 随机访存, 共享内存实现高性能IPC, 简化编程.
- MMAP的缺点: 小文件mmap由于页表开销可能会更慢, 写是异步的, 需要msync刷盘.
❓ MMAP的原理
MMAP修改进程的页表, 使其与物理页对应上.
❓ 第一次访问会发生什么? (可以使用MAP_POPULATE预热)
首先、MMAP调用只是创建一个VMA结构体, 没有分配任何物理页，页表项也是空.
接着、第一次访问会触发缺页中断, 切换到内核态执行缺页处理函数, 将磁盘文件加载进 Page Cache, 构建页表, 如果页缓存已存在直接复用, 构建页表映射, 然后返回用户态继续执行.
总结：触发缺页中断, 分配物理内存, 更新页表, 从磁盘加载到 Page Cache.
❓ 为什么对小文件效果差
- mmap固定开销: 
 - 创建VMA涉及内存分配, 锁, 搜索合适的虚拟地址空间; 
 - 页表相关操作, 及时文件已经在 Page Cache 中仍然会走中断处理流程, 更新页表, 可能涉及 TLB 失效.
 - munmap: 释放VMA, flush TLB, 处理脏页.
- 频繁 mmap/munmap 会导致更多 TLB miss
- page fault 比系统调用更重.
❓ MMAP的副作用与限制:
- 物理内存压力, 如果映射文件远大于物理内存, 频繁的随机访问会产生大量的缺页中断和页置换, 性能断崖下跌.
- SIGBUS风险: mmap过程中, 另一进程截断该文件, 直接SIGBUS.
❓ NUMA 架构内存分配不均对 mmap 性能影响.
-> NUMA: CPU被划分多个节点, 每个节点有本地内存. 访问本地内存延迟低, 带宽高. 跨节点访问需要经过 QPI 延迟更高.
-> 危害:
    - 内存倾斜: 所有物理内存都分配在某个节点. -> 远程访问过多, 吞吐骤降.
    - 远程缺页: 如果当前节点内存满了, 内核可能触发本地节点回收, 可能远程节点很闲, 严重性能抖动.
-> 对策:
    - numactl--interleave=all: 启动时, 强制内核将MMAP交错在CPU节点上.避免单点瓶颈.
    - 绑定CPU亲和力, 尽量让处理数据的线程与它的访存在同节点.
-> 工具:
    - numastat -s 展示内核中关于NUMA内存分配统计, 本地节点命中, 被迫去远程节点分配.
    - numastat -p <pid>: 查看内存倾斜.
    - perf: node-loads, node-loads-misses, 本地和跨节点内存加载.
    - perf mem 查看具体延迟和False Sharing.

❓ HugePage
    - 优点: 减少页表项和深度, 减少 TLB Miss. 
    - 缺点: 内存碎片, 不可换出, 导致系统内存吃紧.

📌Note: 别人问你MMAP的时候, 一方面关注MMAP减少拷贝开销, 一方面提到TLB减少TLB MISS, 同时提到NUMA亲和性的影响.

⚖️ Diff: 小文件 vs MMAP
- 小文件 read: 一次上下文切换, 可能有一次线程调度, 一次数据拷贝 (不需要构建页表)
- MMAP: 第一次上下文切换, 并且构建VMA结构体, 第二次访问时会触发缺页中断, 有一次上下文切换, 可能会有线程调度, 构建页表, 锁竞争, 高并发多线程环境, 大量缺页中断会导致严重的锁竞争.

⚖️ Diff: Direct IO vs MMAp
-  MMAP策略交由内核, 不够透明.
- Direct IO: 绕过 Page Cache, 应用自己管理 Buffer Pool, 可以更精准控制落盘时机和缓存淘汰策略.
Direct IO完全不走 Page Cache, 用户缓冲区直接DMA到磁盘, 编程复杂(需要自己处理缓冲区对齐, IO大小对齐, 错误处理), 适合大文件顺序读写, 数据库引擎.

❓ 零拷贝: 尽量减少CPU参与的数据拷贝, 同时减少用户态/内核态上下文切换次数
- sendfile:
    - 传统 read + write: 磁盘 (DMA)-> Page Cache (CPU)-> App (CPU)->  Socket Buffer (DMA) -> 网卡
    - 优化: 磁盘 (DMA)-> Page Cache (引用) -> Socket Buffer (DMA) -> 网卡 
    传统两次CPU拷贝+4次上下文切换, sendfile 0次CPU拷贝+2次上下文切换.
- splice/tee
    - 传统(Socket1->App->Socket2): Socket1 Buffer (CPU) -> App (CPU) -> Socket2 Buffer
    - 优化: Socket1 Buffer (引用) -> pipe (引用) -> Socket2 Buffer
    传统2次CPU拷贝+多次上下文切换, splice 0次拷贝+大幅减少上下文次数.
- mmap
    大幅减少系统调用, 并且减少了一次CPU拷贝.

❓ False Sharing: 多线程修改位于同一 Cache Line 内的不同变量, 导致 Cache Line 不断失效, 触发 RFO 风暴.
通过 alignas(64) 或手动填充使变量分布在不同 Cache Line.
❓ 关于可见性问题
略-之前已整理, 联动起来即可.

❓ 关于伙伴系统和Slab分配器
伙伴系统负责连续物理页分配和回收, 主要解决外部碎片; slab负责小块内存的快速分配, 主要解决内部碎片和小对象频繁分配和初始化开销.(分配时拆分, 释放时合并)
Slab依赖伙伴系统, 当Slab需要更多内存时, 会从伙伴系统申请整页或多页, 然后切分成小对象管理.
❓ 查看命令
- 查看 Buddy 信息 cat /proc/buddyinfo -> 伙伴系统碎片
- 查看 Slab 缓存 cat /proc/slabinfo 或 slabtop -> 内核对象占用, 以及内存泄漏
- 内核配置 CONFIG_SLUB/CONFIG_SLAB/CONFIG_SLOB

[系统I/O]

❓ 普通 read 系统调用流程
- 用户态调用 read()，执行 syscall 切换到内核态.
- VFS 层分发到具体文件系统 
- Page Cache 查找
    a. Cache Hit: 直接拷贝数据到用户 buf
    b. Cache Miss: 分配页框, 调用文件系统读, 准备 bio 请求, 经 IO 调度后(CFQ, Deadline, BFQ) 提交给磁盘驱动, 数据通过DMA加入 Page Cache. 唤醒读线程, 执行数据拷贝.
- 返回用户态, 上下文切换

📌Note: read 不走缺页中断, 防止流水线冲刷, 分支预测, TLB, 构建页表.
📌Note: bio 解耦文件系统和块设备, 支持 Scatter-Gather IO, 支持 IO 调度和队列管理.

❓ epoll vs select
- epoll 注册走的红黑树 O(logN), 一次注册"终身使用", epoll 直接返回就绪链表, 文件描述符无限制
- select 每次调用都需要 O(N) 拷贝监听列表, 处理就绪事件时需要遍历整个监听列表 O(N), 文件描述符有限制
❓ epoll 的两种触发模式
- 水平触发: 只要有数据就通知 -> 容易惊群
- 边缘触发: 只有数据状态变迁才会通知, 配合非阻塞套接字, 需要循环读取, 直到 EAGAIN. 减少了系统调用次数, 提升性能.
❓ epoll 为什么能支持万级并发
- O(1) 事件分发，直接返回就绪事件
- 内核长期维护状态, 无需每次都拷贝一遍监听事件列表
- 事件驱动
- 低内存开销
❓ Reactor vs Proactor
- Reactor: 同步IO, 数据拷贝由用户负责.
- Proactor: 异步IO, 数据拷贝由内核负责, 一切就绪才返回给用户.
Ps:
一般 epoll + Reactor 模型, 简单业务单线程 Reactor, 复杂业务线程池+生产者消费者, TCP服务器主从模型, 主负责 accept, 新连接分发给从处理.
❓ 如果磁盘I/O称为瓶颈, epoll 还有效吗? io_uring
epoll 主要针对网络套接字, 对磁盘IO兼容性差,  磁盘描述符会一直被认为是 ready 状态, 导致 epoll_wait 频繁返回, 实际 read 可能会出现阻塞线程.
io_uring 通过批量IO大幅度降低IO, 以及异步IO极好的解决了这个问题.

🛠️ C10K/C10M 问题演进
从一条连接一个线程  -> 单线程多路复用 -> 高效事件通知 -> 真正的异步完成 -> 内核旁路, 解决内存, CPU切换, O(N)扫描, 系统调用开销, 内核协议栈开销.

🛠️ epoll + REACTOR 遇到的坑
- 边缘触发, 没有读到 EAGAIN 导致数据丢失/饥饿.
- 惊群效应:
    - 4.5 前, 引入 EPOLLEXCLUSIVE 解决部分惊群
    - 现代: 使用 SO_RESUEPORT 在内核层做负载均衡
    - EPOLLONESHOT 
- 内存和句柄爆炸, 连接激增, socket buffer, epoll 节点, 用户态缓冲区占用多, ulimit(nofile), TCP backlog, socket buffer 大小...

❓ Page Cache 的组织结构: 从 Radix Tree 到 XArray
- 核心矛盾: 根据文件偏移快速找到对应的物理页
- 过去: 基数树, 查的快, 高并发下锁竞争严重, 对大内存支持不够优雅.
- 现在: 基数树进化版, 数据结构不变，但使用体验从“树”变成“数组
❓ Readahead 机制
根据局部性原理, 内核会发起更多 bio 请求提前装载数据. 如果发生随机跳转, 内核会迅速缩小甚至关闭预读, 避免浪费. posix_fadvise 打开/关闭预读
❓ writeback 时机
write 只是写到 Page Cache, 变成 dirty page, 输盘由后台内核线程负责:
    - 定时触发: 配置 30s
    - 阈值触发: 硬阈值, 软阈值
    - 手动触发: fsync
❓ buffered IO vs Direct IO
无预读, 无缓存, 严格内存对齐
❓ 如何刷盘:
每个块设备对应一个或多个 wb 线程, 避免单个全局成为瓶颈. 扫描 address_space 中脏页链表, 将他们装换为 bio 请求, 发送给通用快层, 过程中锁定该页, 避免被修改.
如果将 dirty-_ratio 设置很大, 短期内写性能极高, 但一旦触发阈值,  可能导致系统假死, 通常调低 dirty_background_ratio 少食多餐.

❓ SQ/CQ 是什么?
- SQ: 用户态写, 描述我想做什么?
- CQ: 内核态写, 存放结果如何?
❓ 为什么减少系统调用?
- lue
❓ mmap 了什么
- SQ 和 CQ 直接变成共享内存, 用户态写 SQ, 内核态直接能看到, 内核态写 CQ, 用户态也直接能看到
❓ 0拷贝
- 控制路径 0 拷贝
- 数据路径仍需要把磁盘数据拷贝到用户 Buffer 
io_uring 本质优化控制路径, senfile/splice 才是优化 数据路径.
❓ Submission Batching + Completion Batching
- 减少了上下文切换开销 + 指令缓存友好
- 减少了唤醒频率
批量处理在高负载下能极大提升吞吐, 但是低负载下增加长尾延迟, 最好能自适应.

# [线程调度]

❓ 线程和进程的区别是什么?
在Linux中, 线程被视为轻量级线程. 
    - 进程: 资源分配的最小单元, 独立的地址空间, 文件描述符表, 信号处理.
    - 线程: CPU 调度的最小单元, 独立的寄存器状态和栈.
❓ 线程和进程创建时拷贝哪些? 线程共享哪些?
- 都会创建: 内核管理节点的副本, 内核栈, 寄存器上下文, 权限.
- 区别: 进程拷贝页表, 文件描述符表, 信号处理表, 线程引用.
❓ 线程和进程COW时拷贝哪些? 
用户态内存: 全局数据段, 堆, 用户栈.
❓ 关于内核栈
线程/进程在内核空间有一个私有栈, 当程序通过系统调用, 异常或硬中断进入内核态时, CPU 切换该任务的内核栈, 用于保存内核函数的局部变量, 上下文.
- 作用: 保存上下文, 安全防护(带崩内核), 避免互相污染
❓ 线程切换与进程切换的开销区别? 切换 CPU 寄存器, 栈指针, TLB 会发生什么?
- 线程切换: 寄存器状态, 内核栈指针 - 无需刷新 TLB
- 进程切换: 寄存器, 内核栈, 页表 - 导致 TLB 失效, 大量 Cache Miss
❓ 为什么线程切换不刷 TLB?
- 因为线程共享一个页表, 虚拟地址到物理地址映射没变.
❓TLB shootdown
- 核一修改页表, 通知其他核, 通过 IPI 实现, 多核同步开销很大, 所以高性能代码要尽量避免修改内存映射.
❓ CPU调度策略?
- 核心机制: 
    - CFS (SCHED_NORMAL): 追求虚拟时间的绝对公平.
    - 红黑树管理所有可运行任务, 左边界点 vruntime 最小, 调度器总是挑选最左边节点运行.
    - vruntime += delta_exec x NICE_0_LOA / weight // nice 越低, 权重越高, 增长越慢, 拿更多 CPU.
❓ 为什么 vruntime 需要加权? 
为了实现非均匀的公平, 让高优先级进程 vruntime 增长更慢.
❓ 为什么不用 RR ?
RR时间片固定, 太长太短都不合适, CFS 没有固定时间片, 动态计算, 保证在特定周期内, 所有进程都能跑一遍.
❓ sleep 线程为什么被调度
线程休眠时, vruntime 停止增长, 醒来 vruntime 通常远小于一直跑的线程. 为了防止唤醒后霸占 CPU, 将其 vruntime 设置为一个 cfs_rq->min_vruntime 附近的一个值.
- 演进:
    - 背景: CFS 虽然公平, 但是响应速度不可控.
    - 逻辑: 引入截止日志概念, 不仅看谁最饥饿, 还看谁更紧急. 显著降低延迟.
- 实时调度
    - SCHED_FIFO: 先来先服务, 高优先如果不主动放弃/被更高优先级抢占, 就永远占用 CPU. - 极低延迟 
    - SCHED_RR: 同优先级时间片轮转 - 实时性, 多对等任务.
    - SCHED_DEADLINE: 基于任务周期和截止日志, 严格周期控制.
    Note: 实时任务的优先级永远高于普通任务, 代码写的不好可能导致内核卡死陷入死循环. sched_rt_runtime_us 限制. (实时任务跑多久? 普通任务跑多久?)
❓ cgroup与CPU资源相关的配置? 与内存相关的配置? 还有哪些? 
- CPU
    - 权重分配: CFS 的权重, shares, weight 相对值， n:m
    - 硬上限: 配置周期+使用时间.
    - CPU隔离
    - rt_cputime_us
    🧠通过 cpuset 将关键线程和视觉算法绑核, 
        - 关键通信不设置 cpu.max, 设置较高的 shares
        - 视觉处理设置 cpu.max, 防止算法拉崩整车,
    日志不绑核, 设置低 cpu.max 和 cpu.shares
- 内存: 硬上限(OOM), 软上限(可超过, 会回收), SWAP(抖动, 不建议)
- IO: 限制使用带宽
❓ 关于亲和力? 如果将一个高优先级线程绑定到特定核心, 有什么风险
    好处: 维持 Cache 热度, 减少跨核迁移.
    进阶: 使用 isolcpus 将核从通用调度器中隔离, 只跑关键任务.
    NUMA: 内核会尽力让线程跑在它分配内存的 CPU 节点上.
    🧠: 关键线程设为 SCHED_FIFO, 非关键任务留给 CFS. 关键路径使用 cputset 绑核, 防止漂移, 将网卡中断绑核.
-> CPU 隔离与负载均衡冲突
通过绑核实现 CPU 隔离, 使用 isolcpus 防止负载均衡.
    - 配置中断, 将中断导向非隔离核心
    - 停止内核计时器滴答
    - RCU 回调

❓ RCU 
 - Read：随便读, 有写线程正在修改数据, 读线程也可以继续读旧数据.
 - Copy：当写线程要修改数据时, 先复刻一份副本, 在副本上修改.
 - Update：改完后, 原子性的把全局指针指向新副本. 旧数据还没销毁, 因为计数.
 - 宽限期: 
    - Removal: 携浙江旧数据指针替换为新指针. 此时新读读到新数据, 旧读读到旧数据.
    - 宽限期: 写着必须等待, 等待所有 Removal 之前就开始读的线程全部退出临界区.
    - 回收: 确定没人用旧数据, 安全释放 .
Ps: 发生一次上下文切换就说明退出临界区(读临界区禁止阻塞)
PS: 写需要等待宽限期. 
Ps: 读写锁有一个缓存一致性风暴, 随着 CPU 核心数增加, 读写锁开销呈指数上升.
-> 双缓冲: 两个抽屉 A 和 B
    - 读者: 只从 A 拿
    - 写者: 只往 B 写, 写完交换.

❓ 如果两个进程共享COW内存, 其中一个进程修改了数据触发了拷贝, 此时物理内存增加, 但是系统内存刚好耗尽, 会发生什么?
- OOM Killer - min_free_kbytes. // 死锁风险, 如果被杀进程刚好持有某种关键资源, 陷入无尽回收. 预分配, 禁止COW和SWAP, 锁定内存.
❓ 如果一个 SCHED_FIFO 线程阻塞于一个 SCHED_NORMAL 线程持有的锁?
- 优先级反转, 优先级继承协议? PI-Mutex, 临时继承优先级, 执行任务释放锁. pthread_mutexattr_setprotocol 设置, 默认是不支持的. MP + Mutex 都默认打开 .
❓ 双缓冲的可见性:
- 内存序的理解

❓ 实际用途
1.隔离与绑定, isolcpus 隔离大核, 专门跑 FastDDS 监听线程和消息分发线程, 避免负载均衡干扰.
2.规避中断: 将网卡中断和磁盘强行绑定到小核, 确保通信核不会被中断打断.
3.读多写少的场景没有使用 std::mutex, 而是实现简单的双缓冲机制, 配合内存序, 将读段延迟降低到纳秒.
❓ 关于将中断绑定到大核
- 消除上下文切换产生的毛刺, 防止污染大核的 L1/L2 Cache, 及时发生中断风暴也能保证大核的计算周期确定.
- 核间同步开销, 可以使用无锁 Ring Buffer 来解决这个问题.

# [网络协议栈与DPDK]

# [同步与锁]

# [系统调用]

# [中断机制]

[扩展]

[场景]
