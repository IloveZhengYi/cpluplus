[内存管理]

❓ MMAP是什么? 原理? 为什么MMAP比read/write快?
- 是一种内存映射文件的机制, 将文件映射到虚拟地址空间, 使进程可以像内存一般读写文件, 不用频繁read/write系统调用 .
❓ 传统read/write的缺点:
- 涉及2次内存拷贝: 磁盘 -> 内核态(Page Cache) -> 用户态 (反之亦然), 涉及到多次用户态和内核态切换, 效率低.
- MMAP的优点: mmap消除了page cache到用户空间的额外拷贝, 随机访存, 共享内存实现高性能IPC, 简化编程.
- MMAP的缺点: 小文件mmap由于页表开销可能会更慢, 写是异步的, 需要msync刷盘.
❓ MMAP的原理
MMAP修改进程的页表, 使其与物理页对应上.
❓ 第一次访问会发生什么? (可以使用MAP_POPULATE预热)
首先、MMAP调用只是创建一个VMA结构体, 没有分配任何物理页，页表项也是空.
接着、第一次访问会触发缺页中断, 切换到内核态执行缺页处理函数, 将磁盘文件加载进 Page Cache, 构建页表, 如果页缓存已存在直接复用, 构建页表映射, 然后返回用户态继续执行.
总结：触发缺页中断, 分配物理内存, 更新页表, 从磁盘加载到 Page Cache.
❓ 为什么对小文件效果差
- mmap固定开销: 
 - 创建VMA涉及内存分配, 锁, 搜索合适的虚拟地址空间; 
 - 页表相关操作, 及时文件已经在 Page Cache 中仍然会走中断处理流程, 更新页表, 可能涉及 TLB 失效.
 - munmap: 释放VMA, flush TLB, 处理脏页.
- 频繁 mmap/munmap 会导致更多 TLB miss
- page fault 比系统调用更重.
❓ MMAP的副作用与限制:
- 物理内存压力, 如果映射文件远大于物理内存, 频繁的随机访问会产生大量的缺页中断和页置换, 性能断崖下跌.
- SIGBUS风险: mmap过程中, 另一进程截断该文件, 直接SIGBUS.
❓ NUMA 架构内存分配不均对 mmap 性能影响.
-> NUMA: CPU被划分多个节点, 每个节点有本地内存. 访问本地内存延迟低, 带宽高. 跨节点访问需要经过 QPI 延迟更高.
-> 危害:
    - 内存倾斜: 所有物理内存都分配在某个节点. -> 远程访问过多, 吞吐骤降.
    - 远程缺页: 如果当前节点内存满了, 内核可能触发本地节点回收, 可能远程节点很闲, 严重性能抖动.
-> 对策:
    - numactl--interleave=all: 启动时, 强制内核将MMAP交错在CPU节点上.避免单点瓶颈.
    - 绑定CPU亲和力, 尽量让处理数据的线程与它的访存在同节点.
-> 工具:
    - numastat -s 展示内核中关于NUMA内存分配统计, 本地节点命中, 被迫去远程节点分配.
    - numastat -p <pid>: 查看内存倾斜.
    - perf: node-loads, node-loads-misses, 本地和跨节点内存加载.
    - perf mem 查看具体延迟和False Sharing.

❓ HugePage
    - 优点: 减少页表项和深度, 减少 TLB Miss. 
    - 缺点: 内存碎片, 不可换出, 导致系统内存吃紧.

📌Note: 别人问你MMAP的时候, 一方面关注MMAP减少拷贝开销, 一方面提到TLB减少TLB MISS, 同时提到NUMA亲和性的影响.

⚖️ Diff: 小文件 vs MMAP
- 小文件 read: 一次上下文切换, 可能有一次线程调度, 一次数据拷贝 (不需要构建页表)
- MMAP: 第一次上下文切换, 并且构建VMA结构体, 第二次访问时会触发缺页中断, 有一次上下文切换, 可能会有线程调度, 构建页表, 锁竞争, 高并发多线程环境, 大量缺页中断会导致严重的锁竞争.

⚖️ Diff: Direct IO vs MMAp
-  MMAP策略交由内核, 不够透明.
- Direct IO: 绕过 Page Cache, 应用自己管理 Buffer Pool, 可以更精准控制落盘时机和缓存淘汰策略.
Direct IO完全不走 Page Cache, 用户缓冲区直接DMA到磁盘, 编程复杂(需要自己处理缓冲区对齐, IO大小对齐, 错误处理), 适合大文件顺序读写, 数据库引擎.

❓ 零拷贝: 尽量减少CPU参与的数据拷贝, 同时减少用户态/内核态上下文切换次数
- sendfile:
    - 传统 read + write: 磁盘 (DMA)-> Page Cache (CPU)-> App (CPU)->  Socket Buffer (DMA) -> 网卡
    - 优化: 磁盘 (DMA)-> Page Cache (引用) -> Socket Buffer (DMA) -> 网卡 
    传统两次CPU拷贝+4次上下文切换, sendfile 0次CPU拷贝+2次上下文切换.
- splice/tee
    - 传统(Socket1->App->Socket2): Socket1 Buffer (CPU) -> App (CPU) -> Socket2 Buffer
    - 优化: Socket1 Buffer (引用) -> pipe (引用) -> Socket2 Buffer
    传统2次CPU拷贝+多次上下文切换, splice 0次拷贝+大幅减少上下文次数.
- mmap
    大幅减少系统调用, 并且减少了一次CPU拷贝.

❓ False Sharing: 多线程修改位于同一 Cache Line 内的不同变量, 导致 Cache Line 不断失效, 触发 RFO 风暴.
通过 alignas(64) 或手动填充使变量分布在不同 Cache Line.
❓ 关于可见性问题
略-之前已整理, 联动起来即可.

❓ 关于伙伴系统和Slab分配器
伙伴系统负责连续物理页分配和回收, 主要解决外部碎片; slab负责小块内存的快速分配, 主要解决内部碎片和小对象频繁分配和初始化开销.(分配时拆分, 释放时合并)
Slab依赖伙伴系统, 当Slab需要更多内存时, 会从伙伴系统申请整页或多页, 然后切分成小对象管理.
❓ 查看命令
- 查看 Buddy 信息 cat /proc/buddyinfo -> 伙伴系统碎片
- 查看 Slab 缓存 cat /proc/slabinfo 或 slabtop -> 内核对象占用, 以及内存泄漏
- 内核配置 CONFIG_SLUB/CONFIG_SLAB/CONFIG_SLOB

[系统I/O]

❓ 普通 read 系统调用流程
- 用户态调用 read()，执行 syscall 切换到内核态.
- VFS 层分发到具体文件系统 
- Page Cache 查找
    a. Cache Hit: 直接拷贝数据到用户 buf
    b. Cache Miss: 分配页框, 调用文件系统读, 准备 bio 请求, 经 IO 调度后(CFQ, Deadline, BFQ) 提交给磁盘驱动, 数据通过DMA加入 Page Cache. 唤醒读线程, 执行数据拷贝.
- 返回用户态, 上下文切换

📌Note: read 不走缺页中断, 防止流水线冲刷, 分支预测, TLB, 构建页表.
📌Note: bio 解耦文件系统和块设备, 支持 Scatter-Gather IO, 支持 IO 调度和队列管理.

❓ epoll vs select
- epoll 注册走的红黑树 O(logN), 一次注册"终身使用", epoll 直接返回就绪链表, 文件描述符无限制
- select 每次调用都需要 O(N) 拷贝监听列表, 处理就绪事件时需要遍历整个监听列表 O(N), 文件描述符有限制
❓ epoll 的两种触发模式
- 水平触发: 只要有数据就通知 -> 容易惊群
- 边缘触发: 只有数据状态变迁才会通知, 配合非阻塞套接字, 需要循环读取, 直到 EAGAIN. 减少了系统调用次数, 提升性能.
❓ epoll 为什么能支持万级并发
- O(1) 事件分发，直接返回就绪事件
- 内核长期维护状态, 无需每次都拷贝一遍监听事件列表
- 事件驱动
- 低内存开销
❓ Reactor vs Proactor
- Reactor: 同步IO, 数据拷贝由用户负责.
- Proactor: 异步IO, 数据拷贝由内核负责, 一切就绪才返回给用户.
Ps:
一般 epoll + Reactor 模型, 简单业务单线程 Reactor, 复杂业务线程池+生产者消费者, TCP服务器主从模型, 主负责 accept, 新连接分发给从处理.
❓ 如果磁盘I/O称为瓶颈, epoll 还有效吗? io_uring
epoll 主要针对网络套接字, 对磁盘IO兼容性差,  磁盘描述符会一直被认为是 ready 状态, 导致 epoll_wait 频繁返回, 实际 read 可能会出现阻塞线程.
io_uring 通过批量IO大幅度降低IO, 以及异步IO极好的解决了这个问题.

🛠️ C10K/C10M 问题演进
从一条连接一个线程  -> 单线程多路复用 -> 高效事件通知 -> 真正的异步完成 -> 内核旁路, 解决内存, CPU切换, O(N)扫描, 系统调用开销, 内核协议栈开销.

🛠️ epoll + REACTOR 遇到的坑
- 边缘触发, 没有读到 EAGAIN 导致数据丢失/饥饿.
- 惊群效应:
    - 4.5 前, 引入 EPOLLEXCLUSIVE 解决部分惊群
    - 现代: 使用 SO_RESUEPORT 在内核层做负载均衡
    - EPOLLONESHOT 
- 内存和句柄爆炸, 连接激增, socket buffer, epoll 节点, 用户态缓冲区占用多, ulimit(nofile), TCP backlog, socket buffer 大小...

❓ Page Cache 的组织结构: 从 Radix Tree 到 XArray
- 核心矛盾: 根据文件偏移快速找到对应的物理页
- 过去: 基数树, 查的快, 高并发下锁竞争严重, 对大内存支持不够优雅.
- 现在: 基数树进化版, 数据结构不变，但使用体验从“树”变成“数组
❓ Readahead 机制
根据局部性原理, 内核会发起更多 bio 请求提前装载数据. 如果发生随机跳转, 内核会迅速缩小甚至关闭预读, 避免浪费. posix_fadvise 打开/关闭预读
❓ writeback 时机
write 只是写到 Page Cache, 变成 dirty page, 输盘由后台内核线程负责:
    - 定时触发: 配置 30s
    - 阈值触发: 硬阈值, 软阈值
    - 手动触发: fsync
❓ buffered IO vs Direct IO
无预读, 无缓存, 严格内存对齐
❓ 如何刷盘:
每个块设备对应一个或多个 wb 线程, 避免单个全局成为瓶颈. 扫描 address_space 中脏页链表, 将他们装换为 bio 请求, 发送给通用快层, 过程中锁定该页, 避免被修改.
如果将 dirty-_ratio 设置很大, 短期内写性能极高, 但一旦触发阈值,  可能导致系统假死, 通常调低 dirty_background_ratio 少食多餐.

❓ SQ/CQ 是什么?
- SQ: 用户态写, 描述我想做什么?
- CQ: 内核态写, 存放结果如何?
❓ 为什么减少系统调用?
- lue
❓ mmap 了什么
- SQ 和 CQ 直接变成共享内存, 用户态写 SQ, 内核态直接能看到, 内核态写 CQ, 用户态也直接能看到
❓ 0拷贝
- 控制路径 0 拷贝
- 数据路径仍需要把磁盘数据拷贝到用户 Buffer 
io_uring 本质优化控制路径, senfile/splice 才是优化 数据路径.
❓ Submission Batching + Completion Batching
- 减少了上下文切换开销 + 指令缓存友好
- 减少了唤醒频率
批量处理在高负载下能极大提升吞吐, 但是低负载下增加长尾延迟, 最好能自适应.

# [线程调度]

❓ 线程和进程的区别是什么?
在Linux中, 线程被视为轻量级线程. 
    - 进程: 资源分配的最小单元, 独立的地址空间, 文件描述符表, 信号处理.
    - 线程: CPU 调度的最小单元, 独立的寄存器状态和栈.
❓ 线程和进程创建时拷贝哪些? 线程共享哪些?
- 都会创建: 内核管理节点的副本, 内核栈, 寄存器上下文, 权限.
- 区别: 进程拷贝页表, 文件描述符表, 信号处理表, 线程引用.
❓ 线程和进程COW时拷贝哪些? 
用户态内存: 全局数据段, 堆, 用户栈.
❓ 关于内核栈
线程/进程在内核空间有一个私有栈, 当程序通过系统调用, 异常或硬中断进入内核态时, CPU 切换该任务的内核栈, 用于保存内核函数的局部变量, 上下文.
- 作用: 保存上下文, 安全防护(带崩内核), 避免互相污染
❓ 线程切换与进程切换的开销区别? 切换 CPU 寄存器, 栈指针, TLB 会发生什么?
- 线程切换: 寄存器状态, 内核栈指针 - 无需刷新 TLB
- 进程切换: 寄存器, 内核栈, 页表 - 导致 TLB 失效, 大量 Cache Miss
❓ 为什么线程切换不刷 TLB?
- 因为线程共享一个页表, 虚拟地址到物理地址映射没变.
❓TLB shootdown
- 核一修改页表, 通知其他核, 通过 IPI 实现, 多核同步开销很大, 所以高性能代码要尽量避免修改内存映射.
❓ CPU调度策略?
- 核心机制: 
    - CFS (SCHED_NORMAL): 追求虚拟时间的绝对公平.
    - 红黑树管理所有可运行任务, 左边界点 vruntime 最小, 调度器总是挑选最左边节点运行.
    - vruntime += delta_exec x NICE_0_LOA / weight // nice 越低, 权重越高, 增长越慢, 拿更多 CPU.
❓ 为什么 vruntime 需要加权? 
为了实现非均匀的公平, 让高优先级进程 vruntime 增长更慢.
❓ 为什么不用 RR ?
RR时间片固定, 太长太短都不合适, CFS 没有固定时间片, 动态计算, 保证在特定周期内, 所有进程都能跑一遍.
❓ sleep 线程为什么被调度
线程休眠时, vruntime 停止增长, 醒来 vruntime 通常远小于一直跑的线程. 为了防止唤醒后霸占 CPU, 将其 vruntime 设置为一个 cfs_rq->min_vruntime 附近的一个值.
- 演进:
    - 背景: CFS 虽然公平, 但是响应速度不可控.
    - 逻辑: 引入截止日志概念, 不仅看谁最饥饿, 还看谁更紧急. 显著降低延迟.
- 实时调度
    - SCHED_FIFO: 先来先服务, 高优先如果不主动放弃/被更高优先级抢占, 就永远占用 CPU. - 极低延迟 
    - SCHED_RR: 同优先级时间片轮转 - 实时性, 多对等任务.
    - SCHED_DEADLINE: 基于任务周期和截止日志, 严格周期控制.
    Note: 实时任务的优先级永远高于普通任务, 代码写的不好可能导致内核卡死陷入死循环. sched_rt_runtime_us 限制. (实时任务跑多久? 普通任务跑多久?)
❓ cgroup与CPU资源相关的配置? 与内存相关的配置? 还有哪些? 
- CPU
    - 权重分配: CFS 的权重, shares, weight 相对值， n:m
    - 硬上限: 配置周期+使用时间.
    - CPU隔离
    - rt_cputime_us
    🧠通过 cpuset 将关键线程和视觉算法绑核, 
        - 关键通信不设置 cpu.max, 设置较高的 shares
        - 视觉处理设置 cpu.max, 防止算法拉崩整车,
    日志不绑核, 设置低 cpu.max 和 cpu.shares
- 内存: 硬上限(OOM), 软上限(可超过, 会回收), SWAP(抖动, 不建议)
- IO: 限制使用带宽
❓ 关于亲和力? 如果将一个高优先级线程绑定到特定核心, 有什么风险
    好处: 维持 Cache 热度, 减少跨核迁移.
    进阶: 使用 isolcpus 将核从通用调度器中隔离, 只跑关键任务.
    NUMA: 内核会尽力让线程跑在它分配内存的 CPU 节点上.
    🧠: 关键线程设为 SCHED_FIFO, 非关键任务留给 CFS. 关键路径使用 cputset 绑核, 防止漂移, 将网卡中断绑核.
-> CPU 隔离与负载均衡冲突
通过绑核实现 CPU 隔离, 使用 isolcpus 防止负载均衡.
    - 配置中断, 将中断导向非隔离核心
    - 停止内核计时器滴答
    - RCU 回调

❓ RCU 
 - Read：随便读, 有写线程正在修改数据, 读线程也可以继续读旧数据.
 - Copy：当写线程要修改数据时, 先复刻一份副本, 在副本上修改.
 - Update：改完后, 原子性的把全局指针指向新副本. 旧数据还没销毁, 因为计数.
 - 宽限期: 
    - Removal: 携浙江旧数据指针替换为新指针. 此时新读读到新数据, 旧读读到旧数据.
    - 宽限期: 写着必须等待, 等待所有 Removal 之前就开始读的线程全部退出临界区.
    - 回收: 确定没人用旧数据, 安全释放 .
Ps: 发生一次上下文切换就说明退出临界区(读临界区禁止阻塞)
PS: 写需要等待宽限期. 
Ps: 读写锁有一个缓存一致性风暴, 随着 CPU 核心数增加, 读写锁开销呈指数上升.
-> 双缓冲: 两个抽屉 A 和 B
    - 读者: 只从 A 拿
    - 写者: 只往 B 写, 写完交换.

❓ 如果两个进程共享COW内存, 其中一个进程修改了数据触发了拷贝, 此时物理内存增加, 但是系统内存刚好耗尽, 会发生什么?
- OOM Killer - min_free_kbytes. // 死锁风险, 如果被杀进程刚好持有某种关键资源, 陷入无尽回收. 预分配, 禁止COW和SWAP, 锁定内存.
❓ 如果一个 SCHED_FIFO 线程阻塞于一个 SCHED_NORMAL 线程持有的锁?
- 优先级反转, 优先级继承协议? PI-Mutex, 临时继承优先级, 执行任务释放锁. pthread_mutexattr_setprotocol 设置, 默认是不支持的. MP + Mutex 都默认打开 .
❓ 双缓冲的可见性:
- 内存序的理解

❓ 实际用途
1.隔离与绑定, isolcpus 隔离大核, 专门跑 FastDDS 监听线程和消息分发线程, 避免负载均衡干扰.
2.规避中断: 将网卡中断和磁盘强行绑定到小核, 确保通信核不会被中断打断.
3.读多写少的场景没有使用 std::mutex, 而是实现简单的双缓冲机制, 配合内存序, 将读段延迟降低到纳秒.
❓ 关于将中断绑定到大核
- 消除上下文切换产生的毛刺, 防止污染大核的 L1/L2 Cache, 及时发生中断风暴也能保证大核的计算周期确定.
- 核间同步开销, 可以使用无锁 Ring Buffer 来解决这个问题.

# [网络协议栈与DPDK]

❓ 一个数据包的一生, 内核子系统交互理解.(需深入硬中断, 软中断, skb 与驱动轮询机制)
- 发送: 
    - 第一阶段: app 调用 send/write, 内核先通过五元组找到对应 socket, 从发送缓存区中申请内存, 将数据从用户态拷贝至内核态.
    ❓ 第一个性能瓶颈, 可以通过 sendfile, mmap 消除这次拷贝, 或者用 MSG_ZEROCOPY 让内核直接映射用户态物理页, 实现真正的零拷贝.
    - 第二阶段: 传输层封装, TCP 头, 序列号, 滑动窗口控制. 通过 nf-local_out 后路由决策, 然后进入 nf-POSTROUTING
    - 第三阶段: 邻居子系统与二层封装: 获取下一跳 mac 地址, 填充以太网头
    - 第四阶段: 流控和排队规则, tc-egress, 内核根据算法对包进行排队和丢弃.
        ❓ 锁竞争: 传统 Qdisc 有一把全局大锁, 高并发场景下大家都在抢这把大锁.
        - 解决方案: 使用网卡多队列结合XPS, 将特定CPU绑定特定硬件发送队列, 彻底消除全局锁竞争.
    - 第五阶段: 内核调用 NIC 驱动发送函数, 驱动通过 DMA 将 skb 地址填入 ring buffer, 发送完成网卡触发中断, 驱动异步释放 skb 内存.
- 接收: 
    - 第一阶段: 物理层 (网卡到内存): 
        - 数据到达: NIC 通过 MAC 地址过滤, 确认并接收属于自己的包.
        - DMA 拷贝: NIC 通过 DMA 将数据写入预先分配的内核 ring buffer.
        - 触发硬中断: NIC 向 CPU 发送一个硬件中断信号.
    - 第二阶段: 内核触发器 (硬中断处理):
        - 执行 top half: CPU 响应硬中断, 执行网卡驱动注册的中断处理函数.
        - 屏蔽中断: 为了防止中断嵌套导致 cpu 瘫痪, 驱动会暂时关闭该网卡的中断触发.
        - 调度软中断: 硬中断处理函数启动 NAPI 机制, 将网卡 napi_struct 对象挂载到当前 CPU 的 poll_list 上, 触发 NET_RX_SOFTIRQ 软中断.
        ❓ 为什么不直接在硬中断中处理包?
        硬中断会关全局中断, 处理太慢会导致系统丢失响应, 软中断可以异步执行, 且允许被其他硬中断打断.
    - 第三阶段: NAPI 轮询与协议栈入口(内核)
        - ksoftirqd 执行: 内核线程 ksoftirqd 检测到软中断信号, 调度 net_rx_action.
        - poll 轮询: 调用网卡驱动层提供的 poll 函数, 从 ring buffer 中批量取出数据.
        - skb 封装: 驱动将原始数据封装成 skb
        - 进入通用协议栈: 调用 netif_receive_skb, 进入通用网络层.
            - xdp hook: 加载 xdp 程序, 包在这被拦截或处理.
            - tc: 处理入栈流量控制.
    - 第四阶段: 层层剥壳
        - 网络层: 进入 ip_rcv 进行校验和检查, 分片组装, 路由决定是本地接收还是转发. netfilter (PREROUTING)
        - 传输层: 如果是发往本地, 根据五元组查找 socket, TCP 状态处理, 放入 socket 接收队列.
    - 第五阶段: 临门一脚
        - 唤醒进程: 内核唤醒阻塞在 read 或 recv 上的用户进程.
        - 数据拷贝: 将数据从内核空间的 skb 缓冲区拷贝至 用户 buff.
        - 释放内存: 拷贝完成, 内核释放 skb 和 ring buffer.
❓ 上下文切换和性能瓶颈?
- 瓶颈 A: 中断. 包量巨大, CPU 频繁在执行代码和处理中断间切换, 导致吞吐量下降.
- 瓶颈 B: 拷贝. CPU 密集型操作, 大流量时, 内存带宽和 CPU 周期都消耗在拷贝数据上.
- 瓶颈 C: 锁竞争. 多核环境, 所有核竞争处理一个 socket, 会产生激烈的竞争.
❓ 如何优化?
- kernel bypass: dpdk 直接绕过内核协议栈, 数据通过 DMA 直接进入用户态内存, 完全跳过二到四阶段.
- Zero Copy: 使用 mmap 让用户态和内核态共享一块内存, 省去拷贝开销.
- xdp: 第三阶段 poll 后直接丢弃, 不走后面的阶段.
❓ 关于透明代理, 你是在哪里做拦截包做透明代理的?
我是在 TC 级别挂载了 ebpf 拦截, skb 已经构建完成, 可以方便读写 ip 头和 mac 地址进行重定向, 而不需要像 xdp 那样手动解析原始字节流.
❓ tc 和 xdp 位置区别?
- xdp 在 skb 创建前执行, 适合做急速丢包和急速转发, tc 在 skb 创建后, 支持双向拦截, 实现业务逻辑, 状态感知的最优解.
- xdp 丢弃或转发的报文, 永远不会产生 skb_buff 的 CPU 开销.
❓ 熟练手绘这张图, 指出每一个 hook 点位置(netfilter 的5个位置)
- xdp: DMA 写入 ring buffer, 但还没有封装成 skb.
- tc-ingress: 标准 skb 对象, 丰富的协议上下文, 透明代理, QoS.
- nf-prerouting: 进入 ip 第一个 hook, 没有经过路由决策.
- nf-local-in: 路由发往本机
- 抓包在 xdp, tc 之后, 进 ip 层以前.
- 发送抓包是在 iptables, tc 之后, 交给驱动之前.
❓ ring buffer vs socket buffer 的区别
- ring buffer: 驱动程序预先申请并填充的, NIC 通过 DMA 填充. 溢出后, NIC 会丢弃新来的包, 可以通过 ifconfig/ ethtool -s 看到. 可以通过  ethtool -G eth0 rx 4096 增大队列长度以应对突发流量.
- socket buffer: 复杂的 skb, 驱动从 ring buffer 取出数据并组装成 skb, skb 在协议栈中传递, skb 最终被挂载到接收队列. 溢出后, tcp 会通过滑动窗口通知对方减速, udp 直接丢包, 通过 sysctl 可以配置.
📌Note: ring buffer -> skb 不是拷贝, 是封装.
❓ 关于 TSO/GSO
报文 64K, MTU 1500, 内核不会在 ip 层直接分片, 而是打上标记, 知道驱动, 甚至网卡层才分片. 极大减少处理次数降低 CPU 消耗.
❓ 关于透明代理网桥
利用 TC ingress 识别出要分析的流量, 然后 skb->pkt_type = PACKET_HOST, 在 ebpf 中给 skb 打标记, 配合 iptables 规则路由路由到指定端口.
如果需要直接通过 bpf_redirect 转发.

❓ 广播和多播是如何过滤? 会对性能产生什么影响? 结合 SPDP 协议
- 第一层: NIC 通过 MAC 进行初筛.
- 第二层: 网卡驱动精确 MAC 匹配, 过滤不属于本机的 MAC 多播包.
- 第三层: 内核协议栈, 检查 IP 是否是首先广播地址, 多播地址, 单播地址.
❓ 优化
直接在 xdp 层过滤, 检查以太网头, 避免让包走完整个协议栈再丢弃.

❓ NAPI 机制与中断风暴优化
Ps: 混合中断与轮询, 理解 poll 虚函数, napi_struct 以及 softirq(软中断) 在收包中的调度逻辑.
防止 CPU 陷入中断风暴, NAPI 本质就是: 第一个包由中断触发, 后续通过内核轮询.
napi_struct 是每个网卡对应的调度实体, 包含一个 poll 函数和状态位 state.
poll 虚函数实现从 ring buffer 批量取包, 限制了权重防止独占 CPU.
硬中断只做了一件事, 将 napi_struct 挂载到 poll_list，触发软中断.
内核线程 ksoftirqd 在软中断上下文遍历 poll_list, 循环执行 napi->poll().
❓ ksoftirqd 跑到 100% 怎么办?
- 查看 /proc/softrirqs, 如果单一核心过载, 说明 RSS 没开或者哈希不均.
- 优化手段: RSS/RFS, 由软件进行负载均衡, 通过 IPI 进行分发.

❓ sk_buff(skb) 的内存艺术
Ps: 深入理解 skb 的分配, 克隆(clone), 与拷贝(copy) 开销.
尽量复用内存, 避免 memcpy
skb 并发连续内存块, 主要由两段组成: 描述符(存储元数据), 数据区.

❓ TCP 状态机与性能优化
Ps: 三次握手/四次挥手, Backlog 溢出处理, TIME_WAIT 的产生原因对高性能并发系统的影响.
Ps: 拥塞控制, BBR CUBIC 弱网场景下的表现.
内核两个关键队列: 半连接队列, 全连接队列.
    - 容易造成 SYN Flood, 通过 net.ipv4.tcp_syncookies 避免存储半连接.
    - 如果 app 处理 accept 太慢, 队列满后, 默认丢弃 ACK 包. netstat -s | grep listen queue overflow.
TIME_WAIT: 主动关闭一方进入 TIME_WAIT, 持续 2 x 2MSL:
    - 避免就连接延迟包干扰新连接
    - 确保被动关闭方收到最后的 ACK.
高性能下的缺点: 端口耗尽, 无法建立新连接.
    - 通过 tcp_tw_reuse 通过 timestamps 允许复用.
    - 长连接
❓ 拥塞控制
- CUBIC: 基于丢包的反馈, 不断增加发送窗口, 直到发生丢包, 然后剧烈减小窗口. 弱网表现极差, 吞吐量断崖下跌, 长肥管道恢复太慢.
- BBR: 基于模型的反馈, 不再关注丢包, 实时测试 RTT 和最大带宽, 企图让发送速率刚好等于链路瓶颈带宽. 弱网表现极强, 因为他能区分随机损耗和真实拥塞.
❓ BBR 为什么会引入重传率上升?
BBR 为了探测最大带框, 会周期加大发送速率, 引起丢包, 但 BBR 会迅速回归.

❓ 零拷贝 sendfile mmap 以及内核最新的 MSG_ZEROCOPY 原理.
MSG_ZEROCOPY 不拷贝数据, 允许将用户页 pin 住, 内核直接引用并通过 DMA 发送. 发送完成, 用户缓冲不能立马释放, 必须接收内核发送的完成通知, 才 ok.
适合高吞吐网络应用, 有页面 pin 和完成通知, 编程复杂. lo 好像会 fallback 到拷贝.

❓ 中断风暴与软中断饱和: 如果 CPU 核心的 ksoftirqd 占用 100%, 如何定位并优化? (RSS/RPS/RFS 配置与原理)
1.查看 /proc/softirqs, 观察是哪个核心 NET_RX 增长最快.
2.定位热点函数, 使用 perf 采样, 看看 CPU 在忙什么
3.查看网卡统计, ethtool -S <eth0> 是否开始丢包.
- RSS: 硬件分流, 根据数据包五元组计算哈希, 分到不同硬件队列, 每个队列对应不同硬中断和CPU核心
- RPS: 软件分流, RSS 分流不均, 通软件哈希, 将 skb 通过 IPI 分发到其他 cpu 的 backlog.
- RFS: 增加缓存局部性, 提升 L1/L2 Cache 命中率.
- 网卡硬中断绑核
- NAPI 权重修改
- 避免惊群和CPU隔离

❓ skb 的内存碎片问题: 大量小包场景下, skb 的频繁申请释放如何优化? 内核是如何通过 skb_shared_info 提高效率的?
- 当一个CPU释放skb时, 现放回 CPU 本地缓存, 下次申请从 L1/L2 取, 减少对全局 SLUB 锁竞争.
- 驱动预先分配一组连续物理页, 管理成池, 避免频繁调用 __get_free_pages
- GRO 优化, 减少 skb 描述符数量. (接收)
- 分片聚合, frags 数组支持不连续物理页. 到时通过 DMA 直接发送
- 克隆机制: 赋值描述符, 避免拷贝数据区.
- GSO/TSO: 告诉网卡进行切片.

❓ netfilter 性能损耗: 高性能网关中, 为何 iptables 会成为瓶颈? 引入 ebpf/xdp 绕过 netfilter 的 hook 点
- iptables 是基于规则链, 逐条匹配规则. 线性增长
- 协议栈的上下文沉重
- ebpf/xdp 比 netfilter 更加轻量.

❓ PMD 轮询模式: 为什么 DPDK 必须占用 CPU? 
消除中断开销和上下文切换开销. 确保处理延迟的确定性和中断嵌套的抖动.

❓ 内存池优化: 为什么 dpdk 需要 per-lcore Cache? 如果没有这个缓存, 多核竞争 rte_mempool 会发生什么?
没有本地缓存，大疆会抢同一把锁. 提升 L1/L2 Cache 命中率.

❓ 大流量下的丢包分析: 当 rte_eth_rx_burst 返回 0, 但是网卡统计有丢包, 你如何排查? PCIe 带宽? 内存带宽? 业务代码逻辑处理太慢?
1.网卡硬件丢包: 网卡缓冲区慢丢包 DMA 写内存太慢.
2.NAPI与软中断: ring buffer 溢出(调大), 软中断饱和(CPU 亲和力), 设置权重
4.内核协议栈: perf 查看 nf_hook_slow 函数占比, netstat TCP/UDP 协议栈丢包.
5.使用 eBPF 捕获丢包时内核调用栈, 监控 kfree_skb 调用栈.

❓ xdp vs dpdk
Ps: 控制权 vs 生态: dpdk 绕过了整个内核(协议栈), 性能极高但需要重写应用, 但 xdp 在内核驱动层执行, 可以复用内核路由表和安全策略.
Ps: AF_XDP: 考虑零拷贝模式, 如何兼顾内核生态和高性能.
❓ 在什么场景下你会选择 xdp 而不是 dpdk? 如果业务需要复杂的 tcp 连接状态跟踪, dpdk 的用户态协议栈(vpp or f-stack)会有哪些坑?

❓ 性能调优 
Ps: 给你一台服务器, PPS 上不去, 你会用什么工具?
- perf(定位热点函数), 火焰图, ethtool -s(查看硬件丢包计数), pmu-tools(分析流水线停顿, 如 top-down 模型)
❓ 如何通过 perf 观察 L3 Cache Missing? 如果后端 bound 在内存延迟上, 有哪些代码级优化手段?

❓ 准备一个硬核的案例: 如何通过优化共享内存管理将系统的吞吐提升30%, 或者利用 ebpf 定位一次偶发的网络长尾延迟?
1.使用 ebpf 工具, 统计就绪队列等待 CPU 时间, 如果大量延迟, 可能是因为被内核调度挂起了.
2.内核协议栈黑洞: skb 耗时, kprobe.
3.ethtool 计数打印, 流量突发导致网卡缓存爆了.

❓ ebpf 在网络拦截中的应用
说一下透明代理网关吧

❓ dpdk 的无锁队列实现
这个我太透彻了-略

# [同步与锁]

# [系统调用]

# [中断机制]

[扩展]

[场景]
